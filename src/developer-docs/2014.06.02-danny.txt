720752301


2. Mark Holden NCC
 - remove  the following locations:
	 195866016
	 195866017

	 sql:
		SELECT        TOP (200) PartLocationID, PartID, LocationID, MinQty, RecQty
			FROM            tblPartLocations
			WHERE        (PartLocationID = 195866017)
			
3. Spares > show all orders. Orders contains filter not working.
	- requires proc updating: Order_GetAll_NEW


6. Importing parts file (Microsoft.ACE.OLEDB.12.0 is not registered on the local machine)